# Local developer overrides go here.
# Do NOT set target-cpu=native here — it's committed to git and would affect CI.
# Instead, set it in your shell: export RUSTFLAGS="-C target-cpu=native"
# or use a .cargo/config-local.toml (not tracked by git).
